{% extends '@EasyAdmin/default/layout.html.twig' %}

{% set _content_title = 'Tableau de Board' %}

{% block page_title -%}
    {{ _content_title }}

{%- endblock %}

{% block content_header %}
    <h1 class="title">{{ _content_title }}</h1>
  
    <div class="row">
        <div class="col-md-3 col-sm-6 col-xs-12">
          <div class="info-box">
            <span class="info-box-icon bg-aqua"><i class="ion ion-ios-gear-outline"></i></span>

            <div class="info-box-content">
              <span class="info-box-text">nOMBRE DE SAILLIE</span>
              <span class="info-box-number">
                  {% for nbr in nbr_saillie %}
                  <a href="?entity=Saillie">{{ nbr[loop.index] }}</a> - 
                  <a href="?entity=Bande">{{ nbr.libelle }}</a>
                  {% endfor %}
                  <small></small></span>
            </div>
            <!-- /.info-box-content -->
          </div>
          <!-- /.info-box -->
        </div>
        <!-- /.col -->
        <div class="col-md-3 col-sm-6 col-xs-12">
          <div class="info-box">
            <span class="info-box-icon bg-red"><i class="fa fa-google-plus"></i></span>

            <div class="info-box-content">
              <span class="info-box-text">nOMBRE MIS BAS</span>
              <span class="info-box-number">
                  {% for nbr in nbr_misebas %}
                  <a href="?entity=MiseBas">{{ nbr[loop.index] }}</a>- 
                  <a href="?entity=Bande">{{ nbr.libelle }}</a>
                  {% endfor %}
              </span>
            </div>
            <!-- /.info-box-content -->
          </div>
          <!-- /.info-box -->
        </div>
        <!-- /.col -->

        <!-- /.col -->
        <div class="col-md-3 col-sm-6 col-xs-12">
          <div class="info-box">
            <span class="info-box-icon bg-yellow"><i class="ion ion-ios-people-outline"></i></span>

            <div class="info-box-content">
              <span class="info-box-text">nOMBRE DE LAPINS EN ENGRAISSEMENT</span>
              <span class="info-box-number">
                 {% for nbr in nbr_engraissement %}
                    {% if nbr.nombre_laperaux_vente is null %}
                        {% if nbr.nombre_laperaux is null %} 
                            {{ nbr.nombre_laperaux_sevrage }} -
                        {% else %}
                            {{ nbr.nombre_laperaux }} -
                        {% endif %}    
                    {% else %}
                        {{ nbr.nombre_laperaux_vente }} -
                    {% endif %}
                    
                  <a href="?entity=Bande">{{ nbr.libelle }}</a>
                  {% endfor %}
              </span>
            </div>
            <!-- /.info-box-content -->
          </div>
          <!-- /.info-box -->
        </div>
        <!-- /.col -->
      </div>
    
    
    
{% endblock %}

{% block main %}
   
{% endblock %}